module.exports = {
  user: process.env.MYSQL_USER || 'radius',
  password: process.env.MYSQL_PASSWORD || 'db password',
  database: process.env.DB_NAME || 'radius',
  host: process.env.MYSQL_HOST || 'localhost',
  port: process.env.MYSQL_PORT || 3306
}
